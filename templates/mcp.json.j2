{
  "mcpServers": {
    "tmux-control-plane": {
      "type": "http",
      "url": "http://host.docker.internal:{{ mcp_server_port }}/mcp",
      "headers": {
        "Authorization": "Bearer {{ mcp_bearer_token }}"
      }
    }
  }
}
